from setuptools import setup

setup(name='gym_futbol',
      version='1.0.0',
      install_requires=['gym', 'pymunk', 'numpy', 'matplotlib', 'opencv-python']  # And any other dependencies foo needs
)